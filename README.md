# Hybridencrypt-Communication
主要功能包括：   
（1）利用TCP套接字实现服务器和客户端之间的通信；  
（2）服务器、客户端分别生成RSA密钥对，用RSA加密和传输DH生成的共享密钥；  
（3）服务器生成DH参数并发送给客户端，然后双方交换各自的公钥以生成共享密钥，并从DH共享密钥中派生出HMAC密钥和AES密钥；   
（4）用对称加密（AES-CBC）对数据进行加密并用HMAC验证数据完整和真实性；  
（5）对加密、密钥交换时间，通信延迟，消息数量等参数进行统计；  
（6）利用UI界面，对上述功能进行优化展示。  
